---

- name: install audio tools
  become: yes
  when: ansible_os_family == "Debian"
  apt:
    name: "{{item}}"
    state: latest
  with_items:
    - libasound2    
    - alsa-utils
    - alsa-oss
    

